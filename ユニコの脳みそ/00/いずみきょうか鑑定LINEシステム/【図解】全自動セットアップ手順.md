---
tags: [Setup, Guide, Illustrated, IzumiKyoka, 00]
date: 2026-01-20
source: ユニコの脳みそ/00
aliases: [全自動セットアップ手順, Setup_Guide_Illustrated]
---

[[00_知識マップ]]

# 【図解】全自動鑑定システム セットアップ手順

## 🎯 これから作るもの

お客様がフォーム送信 → 自動でAI鑑定 → 自動でLINE送信

---

## 📝 Step 1：GASにプロンプトをセット（10分）

### 1-1. スプレッドシートを開く

既存のスプレッドシートを開いてください。

### 1-2. Apps Scriptを開く

```
スプレッドシート上部メニュー
  ↓
「拡張機能」をクリック
  ↓
「Apps Script」をクリック
```

新しいタブでGASエディタが開きます。

### 1-3. 新しいスクリプトファイルを作成

```
GASエディタ左側
  ↓
「ファイル」の横の「+」ボタンをクリック
  ↓
「スクリプト」を選択
  ↓
ファイル名を「プロンプト管理」に変更
```

### 1-4. コードを貼り付け

`GAS_完全版_プロンプト管理.gs`を開いて、**全文をコピー**

GASエディタに**貼り付け**

### 1-5. プロンプトを追加

#### 📄 prompt_FREE_001.txt の内容を追加

1. `prompt_FREE_001.txt`を開く
2. **全文をコピー**
3. GASエディタに戻る
4. `getPromptFree001()`関数を探す
5. **【ここにprompt_FREE_001.txtの内容を貼り付け】**の部分を削除
6. コピーした内容を**貼り付け**

**こんな感じになります：**

```javascript
function getPromptFree001() {
  return `# いずみきょうかの溺愛され体験無料鑑定プロンプト

あなたは恋愛占い師「いずみきょうか」として、以下のお客様の無料鑑定を行ってください。

【いずみきょうかのプロフィール】
「我慢してきた恋 → 本命として溺愛される恋へ」導く
恋愛心理学 x 深層リーディング + 溺愛スイッチ
愛されている実感が湧く鑑定が特徴

（...以下続く）
`;
}
```

#### 同じ手順で残り4つも追加

- `getPromptPaid5000()` ← `prompt_PAID_5000.txt`の内容
- `getPromptPaid10000()` ← `prompt_PAID_10000.txt`の内容  
- `getPromptPaid30000()` ← `prompt_PAID_30000.txt`の内容
- `getPromptSubscription()` ← `prompt_SUBSCRIPTION_MONTHLY.txt`の内容

### 1-6. 保存

```
Ctrl + S（Windows）
Command + S（Mac）
```

または、上部の💾アイコンをクリック

---

## 🚀 Step 2：プロンプトをインポート（1分）

### 2-1. 関数を実行

```
GASエディタ上部
  ↓
関数を選択：「importPrompts」
  ↓
「実行」ボタン（▶️）をクリック
```

### 2-2. 権限を許可

初回実行時、権限の確認が出ます：

```
「権限を確認」をクリック
  ↓
Googleアカウントを選択
  ↓
「詳細」をクリック
  ↓
「（プロジェクト名）に移動」をクリック
  ↓
「許可」をクリック
```

### 2-3. 完了！

「プロンプトのインポートが完了しました！」とアラートが表示されます。

### 2-4. 確認

スプレッドシートに戻って、**「ai_prompts」シート**を確認してください。

✅ A列〜H列にデータが入っている
✅ E列（content）に長いプロンプト文が入っている

---

## 🤖 Step 3：AI API連携（5分）

### ChatGPT APIの場合

#### 3-1. APIキーを取得

1. https://platform.openai.com/ にアクセス
2. ログイン
3. 「API keys」→「Create new secret key」
4. キーをコピー（後で使います）

#### 3-2. GASに設定用スクリプトを追加

GASエディタで新しい関数を追加：

```javascript
/**
 * API Keyを設定（一度だけ実行）
 */
function setApiKeys() {
  const scriptProperties = PropertiesService.getScriptProperties();
  
  // ChatGPTを使う場合
  scriptProperties.setProperty('AI_API_TYPE', 'chatgpt');
  scriptProperties.setProperty('OPENAI_API_KEY', 'ここにAPIキーを貼り付け');
  
  Logger.log('API Keyの設定完了');
  SpreadsheetApp.getUi().alert('API Keyの設定完了');
}
```

#### 3-3. 実行

```
関数を選択：「setApiKeys」
  ↓
「実行」ボタンをクリック
```

「API Keyの設定完了」と表示されたらOK！

---

### Claude APIの場合

#### 3-1. APIキーを取得

1. https://console.anthropic.com/ にアクセス
2. ログイン
3. 「API Keys」→「Create Key」
4. キーをコピー

#### 3-2. GASに設定

```javascript
function setApiKeys() {
  const scriptProperties = PropertiesService.getScriptProperties();
  
  // Claudeを使う場合
  scriptProperties.setProperty('AI_API_TYPE', 'claude');
  scriptProperties.setProperty('CLAUDE_API_KEY', 'ここにAPIキーを貼り付け');
  
  Logger.log('API Keyの設定完了');
  SpreadsheetApp.getUi().alert('API Keyの設定完了');
}
```

実行して完了！

---

## 📲 Step 4：LINE連携（既にあるシステムを活用）

既存の`Code.gs`にあるLINE送信関数を使います。

特に追加作業は不要です。

---

## 🎬 Step 5：自動鑑定の実装（10分）

GASエディタで新しいファイルを作成：

```
「+」→「スクリプト」
ファイル名：「自動鑑定」
```

以下のコードを貼り付け：

```javascript
/**
 * ChatGPT APIを呼び出す
 */
function callChatGPT(prompt, customerData) {
  const apiKey = PropertiesService.getScriptProperties().getProperty('OPENAI_API_KEY');
  const url = 'https://api.openai.com/v1/chat/completions';
  
  // プレースホルダーを置換
  const filledPrompt = fillPromptWithData(prompt, customerData);
  
  const payload = {
    model: 'gpt-4',
    messages: [
      {role: 'user', content: filledPrompt}
    ],
    temperature: 0.7,
    max_tokens: 4000
  };
  
  const options = {
    method: 'post',
    contentType: 'application/json',
    headers: {
      'Authorization': 'Bearer ' + apiKey
    },
    payload: JSON.stringify(payload),
    muteHttpExceptions: true
  };
  
  const response = UrlFetchApp.fetch(url, options);
  const result = JSON.parse(response.getContentText());
  
  if (result.error) {
    Logger.log('APIエラー: ' + result.error.message);
    return null;
  }
  
  return result.choices[0].message.content;
}

/**
 * Claude APIを呼び出す
 */
function callClaude(prompt, customerData) {
  const apiKey = PropertiesService.getScriptProperties().getProperty('CLAUDE_API_KEY');
  const url = 'https://api.anthropic.com/v1/messages';
  
  const filledPrompt = fillPromptWithData(prompt, customerData);
  
  const payload = {
    model: 'claude-3-5-sonnet-20241022',
    max_tokens: 8000,
    messages: [
      {role: 'user', content: filledPrompt}
    ]
  };
  
  const options = {
    method: 'post',
    contentType: 'application/json',
    headers: {
      'x-api-key': apiKey,
      'anthropic-version': '2023-06-01'
    },
    payload: JSON.stringify(payload),
    muteHttpExceptions: true
  };
  
  const response = UrlFetchApp.fetch(url, options);
  const result = JSON.parse(response.getContentText());
  
  if (result.error) {
    Logger.log('APIエラー: ' + result.error.message);
    return null;
  }
  
  return result.content[0].text;
}

/**
 * プレースホルダーを実際のデータに置換
 */
function fillPromptWithData(prompt, data) {
  let filled = prompt;
  
  // 基本情報
  filled = filled.replace(/{顧客名}/g, data.name || '');
  filled = filled.replace(/{生年月日}/g, data.birthdate || '');
  filled = filled.replace(/{性別}/g, data.gender || '');
  
  // 無料鑑定用
  filled = filled.replace(/{選択肢}/g, data.choice || '');
  
  // 有料鑑定用
  filled = filled.replace(/{彼の名前}/g, data.hisName || '');
  filled = filled.replace(/{彼の生年月日}/g, data.hisBirthdate || '');
  filled = filled.replace(/{恋愛状況}/g, data.situation || '');
  filled = filled.replace(/{悩みの詳細}/g, data.problem || '');
  filled = filled.replace(/{質問事項}/g, data.question || '');
  filled = filled.replace(/{理想の関係}/g, data.ideal || '');
  
  // 高額鑑定用
  filled = filled.replace(/{出生時刻}/g, data.birthTime || '');
  filled = filled.replace(/{出生地}/g, data.birthPlace || '');
  filled = filled.replace(/{恋愛履歴}/g, data.history || '');
  filled = filled.replace(/{現在の状況}/g, data.current || '');
  filled = filled.replace(/{変わりたい理由}/g, data.reason || '');
  filled = filled.replace(/{願望}/g, data.wish || '');
  
  // サブスク用
  filled = filled.replace(/{配信年月}/g, data.month || '');
  
  // 共通
  filled = filled.replace(/{鑑定日}/g, data.readingDate || new Date().toLocaleDateString('ja-JP'));
  filled = filled.replace(/{祈祷日}/g, data.prayerDate || '');
  
  return filled;
}

/**
 * 自動鑑定のメイン関数
 */
function autoGenerateReading(row) {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const orderSheet = ss.getSheetByName('注文データ'); // シート名は実際の名前に合わせる
  
  // 注文データを取得
  const orderData = orderSheet.getRange(row, 1, 1, orderSheet.getLastColumn()).getValues()[0];
  
  // カラム位置は実際のシートに合わせて調整
  const productId = orderData[2]; // C列：商品ID
  const customerData = {
    name: orderData[3], // D列：名前
    birthdate: orderData[4], // E列：生年月日
    gender: orderData[5], // F列：性別
    // 他のデータも追加
  };
  
  // プロンプトを取得
  const promptData = getPromptByProductId(productId);
  
  if (!promptData) {
    Logger.log('プロンプトが見つかりません: ' + productId);
    return;
  }
  
  // AI APIを呼び出す
  let reading = '';
  const apiType = PropertiesService.getScriptProperties().getProperty('AI_API_TYPE');
  
  if (apiType === 'claude') {
    reading = callClaude(promptData.content, customerData);
  } else {
    reading = callChatGPT(promptData.content, customerData);
  }
  
  if (!reading) {
    Logger.log('鑑定生成に失敗しました');
    return;
  }
  
  // 鑑定結果を保存（適切なカラムに）
  orderSheet.getRange(row, 10).setValue(reading); // J列に保存
  orderSheet.getRange(row, 11).setValue('完了'); // K列にステータス
  
  // LINEで送信（既存の関数を使用）
  const lineId = orderData[6]; // G列：LINE ID
  sendLineMessage(lineId, reading); // 既存のLINE送信関数
  
  Logger.log('鑑定完了: Row ' + row);
}

/**
 * フォーム送信時に自動実行
 */
function onFormSubmit(e) {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const orderSheet = ss.getSheetByName('注文データ');
  const lastRow = orderSheet.getLastRow();
  
  // 最新行の鑑定を自動生成
  autoGenerateReading(lastRow);
}
```

保存してください。

---

## ⚙️ Step 6：トリガー設定（2分）

### 6-1. トリガーを追加

```
GASエディタ左側
  ↓
「トリガー」（時計アイコン）をクリック
  ↓
右下「トリガーを追加」をクリック
```

### 6-2. 設定

```
実行する関数を選択：onFormSubmit
イベントのソース：フォームから
イベントの種類：フォーム送信時
```

「保存」をクリック

---

## ✅ Step 7：テスト（3分）

### 7-1. テスト用データを準備

スプレッドシートの「注文データ」シートに、手動でテストデータを1行追加：

```
商品ID: FREE_001
名前: テスト太郎
生年月日: 1990-01-01
性別: 男性
選択肢: A
LINE ID: （あなたのテスト用LINE ID）
```

### 7-2. 手動で実行

```
GASエディタ
  ↓
関数を選択：autoGenerateReading
  ↓
実行
```

ログを確認：
```
「表示」→「ログ」
```

エラーがなければ成功！

### 7-3. LINEに届いたか確認

指定したLINE IDに鑑定文が届いているはずです。

---

## 🎉 完成！

これで全自動鑑定システムが完成しました！

## 📌 今後の流れ

1. お客様がGoogleフォームから鑑定依頼
2. スプレッドシートに記録
3. トリガーが発動
4. GASが自動で：
   - プロンプトを取得
   - データを埋め込み
   - AI APIを呼び出し
   - 鑑定文を生成
   - LINEで送信
5. お客様が鑑定文を受信

**完全自動化達成！** 🎊

---

## 🆘 トラブルシューティング

### プロンプトが見つからないエラー
→ `ai_prompts`シートのproduct_idが正しいか確認

### APIエラーが出る
→ API Keyが正しいか確認
→ 残高があるか確認

### 鑑定文が途中で切れる
→ max_tokensを増やす（Claude: 8000, GPT: 4000）

### LINEに届かない
→ LINE ID が正しいか確認
→ 既存のLINE送信関数が動いているか確認

---

## 📚 参考

- `セットアップ手順_全自動鑑定システム.md` - 詳細版
- `GAS_完全版_プロンプト管理.gs` - コピペ用
- 各`prompt_xxx.txt` - プロンプト本文

全自動鑑定システム、頑張ってください！💕






